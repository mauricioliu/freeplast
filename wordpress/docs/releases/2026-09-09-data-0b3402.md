# freeplast release 20260909T180647Z-0b3402 (data) — 2026-09-09

Released by wp-release. Target: https://freeplast.mliu.site. Commit: `6e14a7cc111b36b9cfd63a237927f1b707f6b7ac`.
Previous release: 20260909T114639Z-8d8f31 (abdfe0f2).

**Authorization (owner, in session):** libera el tema 1.0.18 a staging

## Chain results

| Phase | Result |
|---|---|
| gate | project gate command passed |
| package | theme:freeplast → 1.0.18; plugin:freeplast-woo → 1.6.8 |
| backup | /root/freeplast-wordpress-backups/20260909T180647Z-0b3402 (restore rehearsal verified, live-check=27) |
| rehearse | on-host — restore verified (27 records) |
| trial | on-host — install against restored copy verified |
| install | artifacts installed under maintenance window |
| verify | INSTALLED-SHA256SUMS verified on target (2 versioned components) |
| fingerprint | pre == post (f9e002472038feea…) |
| smoke | - 200 https://freeplast.mliu.site/
- 200 https://freeplast.mliu.site/tienda/
- 200 https://freeplast.mliu.site/cotizacion/
- 200 https://freeplast.mliu.site/wp-content/themes/freeplast/assets/css/woo.css (byte-identical to committed file) |

## Artifacts

- `freeplast-woo.zip` — `765e3c41680d1612…`
- `freeplast-woo-theme.zip` — `2e9b5d2623c9e8dc…`

## Rollback

`/opt/freeplast-wordpress/bundle/releases/20260909T180647Z-0b3402/rollback.sh — prepared, not executed; fresh --authorized required. code-only (unavailable: no compatible completed bundle) / paired (inspect fingerprint + --acknowledge-data-loss-risk + RESTORE)` — prepared, NOT executed. Run only after an explicit rollback decision.

## What changed and why

Presentation-only polish release of the `freeplast` block theme (1.0.17 →
1.0.18), source commit `6e14a7c`:

- **Home hero restored to the approved v6 treatment** — covered crop on a
  24px tinted panel with the caption as an in-image chip (was a 126px
  letterbox sliver with a plain caption); H1 moved onto the Display scale
  (36 → 40 ≥768 → 48 + fixed break ≥1024 → 60 ≥1280).
- **Cotiza Online band made full-bleed** (v6 band-tint intent; was an
  inset tinted box).
- **Type hierarchy** — section headings and the featured-products heading
  aligned to the Headline scale (30px → 36/40 ≥768); corporate page
  titles and content leads stabilized (content-authored H1 leads rendered
  at 32px UA under a 28px template title).
- **Fixed WP flow-layout sibling-margin leaks** where theme grids own the
  rhythm (Misión/Visión cards were top-staggered on desktop; featured
  heading gap was doubled).
- **Product disclosure affordance** — "Ficha técnica completa" had no
  expand indicator; added a rotating chevron (1.6-stroke language) + hover.
- **Contact copy**: phone normalized to «+56 9 6844 4265» (matches the
  help dialog and PRODUCT.md).

Tier is **data** because the sealed photo-migration closure from 8d8f31
remains configured; the diff itself contains no logic or data changes.
The migration re-applied under its guard during rehearse/trial; the
fingerprint was identical pre/post (`f9e00247…`), consistent with the
expected no-op. A second technical opinion (`.scratch/release-1.0.18/
tier-question.md`) reviewed the tier choice and confirmed data with the
current configuration; retiring the migration is deferred to a separate
scoped transition with its own closure criteria.

## Evidence limits — what these checks do NOT prove

- **No visual acceptance**: every phase above is mechanical. Whether the
  new hero, band, type scale and disclosure *look right* is a human
  screen review on staging, plus the owner's sign-off.
- **Fingerprint equality does not prove zero photo re-imports**: the
  protected-state digest deliberately tolerates authorized photo changes;
  the trial evidence shows the guard accepted the run, not that every
  media row was byte-untouched.
- **Smoke covers four URLs only** (home, tienda, cotizacion, woo.css
  bytes). Product pages, search, 404, the quote form interior and dialog
  JS were exercised locally during development, not re-verified on target
  by this pipeline.
- **Content defects knowingly shipped** (owner-owned WP admin edits,
  flagged): double H1 in the page content of Nosotros/Contacto/Privacidad
  (visually neutralized by CSS), «+ 56 9 …» spacing ×2 in Contacto page
  content, «Contacto/Contáctanos» duplication.
- **Rollback to 8d8f31 is paired-only** (both releases involve the
  migration machinery); code-only rollback is not eligible for this pair.
- Staging remains non-indexed; nothing here authorizes production
  (freeplast.cl untouched, separate authorization required).
