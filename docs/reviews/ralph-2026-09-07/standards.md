Standards-only review of `eb69ec7...433c6a3`: **four findings**. The standalone PHP suite passed 260 assertions; no servers or external services were used.

- **Hard violation — attempt identity is silently discarded.** [freeplast-woo.php:618–620](/home/mauricio-liu/Projects/freeplast/wordpress/wp-content/plugins/freeplast-woo/freeplast-woo.php:618) reads `fpw_attempt` from `get_posted_data()`, but pinned Woo’s [implementation:790–858](/home/mauricio-liu/Projects/freeplast/wordpress/.build/wp/wp-content/plugins/woocommerce/includes/class-wc-checkout.php:790) only includes registered checkout fields; this hidden input is unregistered. Consequently, claims use the session’s current token. An older form that falls through recovery can become a different attempt, violating [BUILD-DECISIONS.md:37–40](/home/mauricio-liu/Projects/freeplast/wordpress/BUILD-DECISIONS.md:37), which requires resubmissions to retain their identity. Register the field in posted-data processing or read and validate the submitted token directly.

- **Hard violation — failed identity verification permits mutation.** [verify-ventas-role.py:139–151](/home/mauricio-liu/Projects/freeplast/wordpress/scripts/verify-ventas-role.py:139) calls `ok('identity_verified', ...)` but ignores its result; `ok()` merely records failure. The following POST can append a technical note to a real request supplied through `--order-id`. This breaches [BUILD-DECISIONS.md:27](/home/mauricio-liu/Projects/freeplast/wordpress/BUILD-DECISIONS.md:27): verify the technical record’s identity before mutation. Abort dependent probes on failure, retaining account cleanup.

- **Hard violation — CSRF rejection still counts as deletion authorization evidence.** [verify-ventas-role.py:188–191](/home/mauricio-liu/Projects/freeplast/wordpress/scripts/verify-ventas-role.py:188) submits the trash route without `_wpnonce`, then reports `delete_denied` because the record survives. This passes even with deletion privileges, contradicting [BUILD-DECISIONS.md:27](/home/mauricio-liu/Projects/freeplast/wordpress/BUILD-DECISIONS.md:27)’s valid-nonce/separate-CSRF evidence rule. Classify this as CSRF coverage; require a valid nonce for authorization evidence.

- **Hard violation; also heuristic Duplicated Code — pending decisions remain separate.** [cart-quantity-feedback.js:157–159](/home/mauricio-liu/Projects/freeplast/wordpress/wp-content/themes/freeplast/assets/js/cart-quantity-feedback.js:157) adds `"return storeBusy || inflight > 0;"`, while **206–207** retains `"return pending.indexOf(key) !== -1 || inflight > 0;"`. Verdicts never call the new predicate, contrary to [BUILD-DECISIONS.md:48](/home/mauricio-liu/Projects/freeplast/wordpress/BUILD-DECISIONS.md:48)’s explicit two-consumer rule. Separate definitions can disagree during other pending operations and require coordinated future fixes. Make verdict deferral consume the shared predicate while preserving its item-specific check.